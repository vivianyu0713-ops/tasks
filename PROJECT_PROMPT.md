# 项目生成提示词：个人工作雷达

下面这段提示词可用于让 AI 重新生成或继续迭代本项目。

```text
你是一个资深全栈产品工程师和前端设计师，请帮我实现一个个人工作任务管理平台，产品名暂定为「个人工作雷达 / Signal Desk」。

## 一、产品背景

我当前使用钉钉多维表格管理工作事项，包括任务、需求、线上问题、逻辑答疑、日常待办等。但多维表格存在几个问题：

1. 入口分散，很多事项来自私聊、群聊、口头沟通，容易遗漏。
2. 不同事项需要关注不同字段，单一通用表单不够好用。
3. 我希望先快速记录，再判断它到底属于需求、线上问题、逻辑答疑、任务还是低价值事项。
4. 我希望保护自己的时间和精力，把不该我做、低价值、职责边界外的事项沉淀出来。
5. 后续我希望能基于业务节点复盘：最近做的需求是否服务于销售漏斗、线索分配、约课、流转、数据治理、AI 提效等方向。

这个工具不是普通 ToDo，而是一个「个人工作接待台 + 任务管理台 + 工作雷达」。

## 二、核心产品模型

请区分这几个概念：

- 页面 = 查看和管理视图
- 所属事项 = 数据属性，可随时调整
- 收集箱 = 快速录入入口
- 详情抽屉 = 修改分类和补充专属信息的地方

不要让“在哪个页面新增”决定事项分类。新增事项时必须有「所属事项」下拉，默认值为「任务」。

所属事项包括：

- 任务
- 需求
- 线上问题
- 逻辑答疑
- 日常待办
- 低价值

注意：底层可以把低价值存成 `不承接`，但 UI 展示建议叫「低价值」，因为它兼顾个人复盘和对领导展示。

## 三、一期核心页面

### 1. 工作台

默认首页是工作台，不是收集表单。

工作台要展示：

- 今日待处理
- 待反馈
- 已超期
- 待分流
- 低价值事项
- 今日聚焦队列
- 工作雷达态势面板

工作台只回答一个问题：今天我该关注什么？

### 2. 收集箱

收集箱是单独页面，用于快速记录。

交互风格要类似 Google 登录页：居中、竖向、聚焦，一个主动作。

收集箱第一屏应该是一个大的输入框：

「把刚收到的事情丢进来」

输入后系统根据关键词做简单推荐：

- 包含“订单、异常、报错、数据不对、线上、bug、失败”时，推荐：线上问题 + 研发排查
- 包含“需求、功能、上线、原型、开发、迭代”时，推荐：需求 + 转需求
- 包含“规则、逻辑、为什么、口径、流程、答疑”时，推荐：逻辑答疑 + 逻辑答复
- 否则默认进入：任务

收集字段包括：

- 标题，可选
- 问题描述，必填
- 所属事项，默认任务
- 处理路径
- 提出人
- 对接人
- 提出时间
- 预期解决时间
- 业务节点
- 下一步动作

### 3. 任务列表

任务是默认池，用于承接还没明确归属的事项。

关注字段：

- 标题
- 所属事项
- 提出人
- 对接人
- 预期解决时间
- 下一步动作
- 状态

### 4. 需求管理

需求页面只展示所属事项为「需求」的数据。

需求列表要支持：

- 表格视图
- 看板视图

需求表格字段：

- 需求名称
- 关联需求文档
- 对接研发
- 预计上线时间
- 实际 / 有效上线时间
- 业务节点
- 状态
- 结论

看板视图先按状态分组即可。

### 5. 线上问题

线上问题页面只展示所属事项为「线上问题」的数据。

关注字段：

- 问题现象
- 影响范围
- 发生时间
- 处理路径
- 排查结论
- 状态
- 是否转需求

### 6. 逻辑答疑

逻辑答疑页面只展示所属事项为「逻辑答疑」的数据。

关注字段：

- 问题
- 提问人
- 涉及规则 / 流程
- 答复结论
- 是否需要沉淀文档
- 状态

### 7. 低价值

低价值页面展示不该继续消耗精力的事项。

低价值原因包括：

- 职责边界外
- 替别人做无效判断
- 信息不完整
- 无明确业务价值
- 重复事项
- 已有规则可自助解决

## 四、详情抽屉

所有列表点击一行后打开右侧详情抽屉。

详情抽屉必须支持修改：

- 所属事项
- 状态
- 处理路径
- 提出人
- 对接人
- 提出时间
- 预期解决时间
- 业务节点
- 结论
- 低价值原因

并根据所属事项展示专属字段：

### 需求专属字段

- 关联需求文档
- 对接研发
- 预计上线时间
- 实际 / 有效上线时间

### 线上问题专属字段

- 影响范围
- 发生时间
- 排查结论
- 是否转需求

### 逻辑答疑专属字段

- 涉及规则 / 流程
- 答复结论
- 是否需要沉淀文档

## 五、数据库和后端实现

使用 Next.js 全栈实现，不需要单独 Python 或 Java 后端。

架构：

浏览器页面
-> Next.js API Routes
-> Supabase HTTPS API
-> Supabase Postgres

不要让浏览器直接访问 Supabase Secret Key。

环境变量：

NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_SECRET_KEY="your-secret-key"

Supabase 表名：work_items

核心字段：

- id
- title
- category
- process_path
- requester
- description
- owner
- reported_at
- expected_done_at
- status
- conclusion
- reject_reason
- business_area
- created_at
- updated_at

为了一期快速交付，可以先用 `description + JSON meta` 的兼容方式存分类专属字段，后续再迁移成独立列或详情表。

需要提供 API：

- GET /api/work-items
- POST /api/work-items
- PATCH /api/work-items/:id
- DELETE /api/work-items/:id

## 六、UI 设计方向

不要做成普通后台或普通表单系统。

视觉定位：个人工作雷达 / Signal Desk。

设计关键词：

- 专业
- 有态势感
- 像个人工作操作系统
- 比钉钉多维表格更有产品感
- 不是花哨动画，而是清晰、顺手、有记忆点

视觉风格：

- 深色侧边栏
- 浅色内容区
- 雾白卡片
- 电光蓝强调色
- 琥珀橙表示风险/超期
- 冷灰表示低价值/边界外

建议色彩：

- 深蓝灰：#0F172A
- 雾白：#F8FAFC
- 电光蓝：#38BDF8
- 琥珀橙：#F97316
- 成功绿：#10B981
- 冷灰：#94A3B8

标志性交互：

- 工作台有一个「工作雷达」面板
- 收集箱是居中竖向的智能录入卡片
- 需求页有专业表格视图和看板视图
- 详情页使用右侧抽屉
- 所属事项和状态使用胶囊标签

## 七、不要做的内容

一期不要做：

- 登录
- 多人协作
- 复杂权限
- AI 真分类
- 钉钉机器人
- 附件上传
- 复杂图表复盘
- 甘特图
- 完整项目管理

复盘是后续扩展，不要在一期主导航中占一级入口。

## 八、验收标准

实现后必须满足：

1. 默认进入页面看到工作台，不是录入表单。
2. 收集箱是独立页面。
3. 收集箱是居中竖向智能录入体验。
4. 新增事项时可以选择所属事项，默认任务。
5. 详情抽屉中可以随时修改所属事项。
6. 需求页面只展示需求，并展示需求专属字段。
7. 需求页面支持表格视图和看板视图。
8. 线上问题和逻辑答疑展示各自专属字段。
9. 低价值作为页面展示名，不使用“不承接”作为主导航文案。
10. 数据真实写入 Supabase。
11. npm run lint 通过。
12. npm run build 通过。
```
